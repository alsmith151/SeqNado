name: test
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - pip
  - pytest
  - pytest-cov
  - pytest-xdist
  - python>=3.11,<3.13
  - pexpect
  - pip:
      - snakemake
      - tracknado
      - wget
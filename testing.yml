name: test
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - pip
  - pytest
  - pytest-cov
  - pytest-xdist
  - python>=3.11
  - pexpect
  - pip:
      - snakemake
      - snakemake-executor-plugin-slurm
      - tracknado
      - wget
